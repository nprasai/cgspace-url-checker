# CGSpace URL Checker

Tool to:
- split multi-value URL columns
- check URL health
- export results

## Install

```bash
pip install -e .

##Run
### Step 1: Go to folder
```bash
cd cgspace_url_checker
### Step 2: Install locally
```bash
pip install -e .

### Step 3:
```bash
cgspace-url-checker \
  --input "your_file.csv" \
  --output-dir "output_folder"
